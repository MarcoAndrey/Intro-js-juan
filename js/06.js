// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.6); //Redondea la cifra
resultado = Math.ceil(2.1); //Redondea siempre hacia arriba
resultado = Math.floor(2.1); //Redondea siempre hacia abajo
resultado = Math.sqrt(777); //Raiz cuadrada
resultado = Math.abs(-200); //De negativo lo pasa a positivo
resultado = Math.min(3, 5, 1, 8, 2, 10); //Nos muestra el minimo numero de un listado
resultado = Math.max(3, 5, 1, 8, 2, 10); //Nos muestra el Maximo numero de un listado
resultado = Math.random(); //Nos muestra un numero random
resultado = Math.floor(Math.random() * 30); //Nos muestra un numero aleatorio

console.log(resultado);
